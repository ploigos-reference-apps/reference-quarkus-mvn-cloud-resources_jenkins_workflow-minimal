############################################
#        THIS FILE IS AUTOGENERATED
#
# ANY MODIFICATIONS TO THIS FILE WILL BE
# OVERWRITTEN BY THE CI/CD PIPELINE
#
# Generation Timestamp: 2020-11-18 18:41:03.171698
############################################
#
# Default values
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
#
#

replicaCount: 1

deploymentNamespace: ref-quarkus-mvn-jenkins-min-fruit-feature-test42-dev

service: ref-quarkus-mvn-jenkins-min-fruit
image:
  repository: quay-quay-enterprise.apps.tssc.rht-set.com/ploigos-ref/ref-quarkus-mvn-jenkins-min-fruit
  tag: 1.0.2-feature_test42
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetport: 8080

probe:
  readypath: /fruits
  livepath:  /fruits

route:
  enabled: True
  annotations: {
  }
  path: /
  hostname: fruit.ref-quarkus-mvn-jenkins-min-fruit-feature-test42-dev.dev.apps.tssc.rht-set.com

  tls: []
  #  - secretName: chart-example-tls
  #    hosts:   #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:   #  cpu: 100m
  #  memory: 128Mi
  # requests:   #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {
}

tolerations: []

affinity: {
}
